# AI 生成圖像偵測系統：實驗流程與程式碼解析

> 本文件完整記錄 FusionDetector + GRL 的實驗設計、模型架構與各程式碼模組的實作細節。

---

## 目錄

1. [專案概覽](#1-專案概覽)
2. [整體架構圖](#2-整體架構圖)
3. [五個特徵流（Feature Streams）詳解](#3-五個特徵流feature-streams詳解)
   - [Stream 1：CLIP 語意特徵](#stream-1clip-語意特徵)
   - [Stream 2：FFT 頻域特徵](#stream-2fft-頻域特徵)
   - [Stream 3：DCT JPEG 壓縮特徵](#stream-3dct-jpeg-壓縮特徵)
   - [Stream 4：DIRE 擴散重建誤差](#stream-4dire-擴散重建誤差)
   - [Stream 5：NoisePrint（SRM）噪聲特徵](#stream-5noiseprintsrm噪聲特徵)
4. [融合模組（Fusion Module）](#4-融合模組fusion-module)
5. [梯度反轉層（GRL）與對抗訓練](#5-梯度反轉層grl與對抗訓練)
6. [訓練流程詳解](#6-訓練流程詳解)
7. [實驗流程（10 個階段）](#7-實驗流程10-個階段)
8. [資料集結構](#8-資料集結構)
9. [關鍵超參數總表](#9-關鍵超參數總表)
10. [程式執行順序](#10-程式執行順序)

---

## 1. 專案概覽

### 研究目標

本專案旨在建立一個**穩健的 AI 生成圖像偵測系統**，能夠：

1. 準確分辨「真實照片」與「AI 生成圖像」（二元分類）
2. 識別圖像來自哪個生成器（來源分類）
3. **對未見過的生成器保持良好的泛化能力**（跨生成器泛化）

### 核心挑戰

- AI 生成技術日新月異，訓練時見過的生成器（如 Stable Diffusion v4）不代表測試時遇到的生成器（如 Midjourney）
- 圖像壓縮、縮放、加噪等後處理會破壞偵測特徵
- 不同生成器的假圖特徵差異極大（GAN vs. Diffusion 的假跡完全不同）

### 解決方案

採用**五流特徵融合 + 梯度反轉層（GRL）**：
- 五個互補的特徵流各自捕捉不同層面的假圖訊號
- Cross-Attention Transformer 讓不同流互相協作
- GRL 強迫模型學到「與生成器無關」的判別特徵

---

## 2. 整體架構圖

```
輸入圖像 (B, 3, H, W)
        │
        ├──────────────────────────────────────────────────────┐
        │                                                        │
   ┌────┴─────┐  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
   │  CLIP    │  │  FFT   │  │  DCT   │  │  DIRE  │  │ Noise  │
   │ViT-L/14  │  │ResNet34│  │ResNet18│  │ResNet50│  │ SRM-30 │
   │  (凍結)  │  │        │  │        │  │  VAE   │  │  CNN   │
   └────┬─────┘  └───┬────┘  └───┬────┘  └───┬────┘  └───┬────┘
        │            │           │            │            │
       512d         512d        512d         512d         512d
        └────────────┴───────────┴────────────┴────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │   Cross-Attention Fusion │
                    │   (2-layer Transformer)  │
                    │   8 heads, d_model=512   │
                    └────────────┬────────────┘
                                 │
                             2560d (5×512)
                                 │
                    ┌────────────▼────────────┐
                    │    Shared Backbone       │
                    │  2560 → 1024 → 512       │
                    │  LayerNorm + GELU        │
                    └────┬─────────────┬───────┘
                         │             │
              ┌──────────▼──┐    ┌─────▼──────────────────┐
              │ Binary Head │    │  [GRL 梯度反轉]         │
              │   512 → 2   │    │  Generator Discriminator│
              │  Real/Fake  │    │       512 → 12          │
              └─────────────┘    └─────────────────────────┘
```

---

## 3. 五個特徵流（Feature Streams）詳解

### 為什麼需要五個流？

每種 AI 生成技術都有獨特的「偽造痕跡」，單一特徵無法覆蓋所有情況：

| 特徵流 | 捕捉的資訊 | AI 假圖的特點 |
|--------|-----------|--------------|
| CLIP   | 語意內容模式 | 語意層面分布不同 |
| FFT    | 頻率分布   | 高頻能量更均勻、更平滑 |
| DCT    | JPEG 壓縮結構 | 缺乏自然壓縮痕跡 |
| DIRE   | VAE 重建誤差 | Diffusion 模型重建誤差極低 |
| Noise  | 噪聲殘差   | 噪聲模式與相機雜訊不同 |

---

### Stream 1：CLIP 語意特徵

**檔案**：`src/feature_extractors/clip_extractor.py`

**模型**：OpenAI CLIP ViT-L/14（權重凍結）

**核心流程**：

```
原始圖像 tensor (B, 3, H, W)
    │
    ▼ _preprocess()：反轉 ImageNet 正規化 → PIL Images
    │
    ▼ CLIP processor（resize to 224×224, CLIP 正規化）
    │
    ▼ CLIP ViT-L/14（凍結）→ 1024d CLS token
    │
    ▼ 可學習線性投影層：1024d → 512d
    │
    ▼ LayerNorm
    │
   512d 輸出
```

**關鍵程式碼解析**：

```python
class CLIPFeatureExtractor(nn.Module):
    def __init__(self):
        # 載入 CLIP 模型（凍結所有參數）
        self.clip_model = CLIPModel.from_pretrained("openai/clip-vit-large-patch14")
        for param in self.clip_model.parameters():
            param.requires_grad = False  # 凍結！不更新 CLIP 本身

        # 可學習的降維投影
        self.projection = nn.Sequential(
            nn.Linear(1024, 512),
            nn.LayerNorm(512)
        )
```

**為何凍結 CLIP？**
CLIP 已在 4 億圖文對上訓練，其 ViT 特徵極具語意表達力。直接凍結可避免過擬合，只需訓練投影層。

**XAI 方法：Attention Rollout**

```python
def get_attention_map(self, image_tensor):
    # 從 CLIP ViT 的每層取出 attention weights
    # 對所有層做矩陣乘積（模擬資訊流動）
    # 輸出 16×16 attention 熱力圖（224px / 14px patch = 16）
```

---

### Stream 2：FFT 頻域特徵

**檔案**：`src/feature_extractors/fft_extractor.py`

**模型**：ResNet34

**核心原理**：AI 生成圖像在頻率域上「太完美」，缺乏自然影像的高頻細節與隨機性。

**FFT 計算流程**：

```
原始解析度圖像（不先 resize！）
    │
    ▼ 轉灰階
    │
    ▼ np.fft.fft2()：二維快速傅立葉變換
    │
    ▼ np.fft.fftshift()：將低頻移至中心
    │
    ▼ log(1 + |magnitude|)：對數正規化，壓縮動態範圍
    │
    ▼ 正規化至 [0, 1]
    │
    ▼ resize 至 224×224
    │
    ▼ 複製為 3 通道
    │
    ▼ ResNet34 → flatten → 512d
```

**關鍵細節**：FFT 在**原始解析度**下計算，避免 resize 破壞高頻資訊。

```python
def _compute_fft(self, img_array):
    gray = cv2.cvtColor(img_array, cv2.COLOR_RGB2GRAY)
    f = np.fft.fft2(gray)
    fshift = np.fft.fftshift(f)               # 低頻移中心
    magnitude = np.abs(fshift)
    log_magnitude = np.log1p(magnitude)       # 對數壓縮
    # 每批次正規化到 [0, 1]
    normalized = (log_magnitude - log_magnitude.min()) / (log_magnitude.max() + 1e-8)
    return normalized
```

---

### Stream 3：DCT JPEG 壓縮特徵

**檔案**：`src/feature_extractors/dct_extractor.py`

**模型**：ResNet18

**核心原理**：JPEG 壓縮使用 8×8 DCT 塊。真實照片經 JPEG 壓縮後有特定的塊狀偽影；AI 圖像通常沒有。

**DCT 計算流程**：

```
灰階圖像
    │
    ▼ 分割為 8×8 不重疊塊（JPEG 標準）
    │
    ▼ 2D DCT：對每塊做 D × block × D^T
       (D = DCT-II 變換矩陣，解析計算)
    │
    ▼ log(1 + |DCT coefficients|)：對數壓縮
    │
    ▼ 重組為空間 DCT map
    │
    ▼ 展開為 3 通道 → ResNet18 → 512d
```

**DCT 矩陣生成**（解析計算，非近似）：

```python
def _get_dct_matrix(self, n=8):
    # DCT-II 基底函數
    # D[0,j] = 1/sqrt(N)
    # D[i,j] = sqrt(2/N) * cos(π(2j+1)i / 2N)  for i>0
    m = np.zeros((n, n))
    for i in range(n):
        for j in range(n):
            if i == 0:
                m[i, j] = 1.0 / np.sqrt(n)
            else:
                m[i, j] = np.sqrt(2.0 / n) * np.cos(np.pi * i * (2*j+1) / (2*n))
    return m
```

---

### Stream 4：DIRE 擴散重建誤差

**檔案**：`src/feature_extractors/dire_extractor.py`

**模型**：ResNet50 + Stable Diffusion VAE

**核心原理**：Diffusion 模型生成的圖像可以被 VAE 以極低誤差重建，因為 VAE 是其生成過程的一部分；真實照片無此特性。

**DIRE 計算流程**：

```
原始圖像 (B, 3, H, W)
    │
    ▼ 正規化至 [-1, 1]（VAE 標準輸入範圍）
    │
    ▼ VAE Encoder：圖像 → latent space (4, H/8, W/8)
    │
    ▼ VAE Decoder：latent → 重建圖像
    │
    ▼ 計算重建誤差：|原始 - 重建|  (逐像素絕對差)
    │
    ▼ 誤差圖 → ResNet50 → 512d
```

**VAE 載入（使用 SD 官方 VAE）**：

```python
def _load_vae(self):
    # 使用 Stable Diffusion 的 VAE（ft-mse 版本，重建質量高）
    self.vae = AutoencoderKL.from_pretrained(
        "stabilityai/sd-vae-ft-mse"
    ).to(self.device)
    # 凍結 VAE（只做推論，不訓練）
    for param in self.vae.parameters():
        param.requires_grad = False
```

**備用方案（VAE 不可用時）**：

```python
# 用 avg_pool2d 模擬低通濾波作為退化代理
reconstructed = F.avg_pool2d(image, 3, stride=1, padding=1)
error = torch.abs(image - reconstructed)
```

**為何比原始 DDIM-DIRE 更快？**
原始 DIRE 需要完整的 DDIM 前向+反向 diffusion（數十步），我們只用 VAE（單次 encode+decode），速度提升 10× 以上。

---

### Stream 5：NoisePrint（SRM）噪聲特徵

**檔案**：`src/feature_extractors/noise_extractor.py`

**模型**：輕量級 CNN（30 通道輸入）

**核心原理**：SRM（Steganalysis Rich Model）是針對影像偽造設計的 30 個高通濾波器組，可提取細微噪聲殘差，揭示相機感測器雜訊或 AI 生成偽影。

**SRM 濾波器（30 個 3×3 核）**：

```
邊緣偵測類：水平、垂直、對角線邊緣
二階微分類：Laplacian 變體
三階微分類：更高階差分
混合類  ：各方向組合
```

**NoisePrint 計算流程**：

```
原始解析度圖像（同 FFT，不先 resize）
    │
    ▼ 轉灰階
    │
    ▼ 30 個固定 SRM 高通濾波器（depthwise convolution）
    │  → 輸出 (B, 30, H, W) 噪聲殘差圖
    │
    ▼ resize 至 224×224
    │
    ▼ 輕量 CNN（Conv+BN+ReLU 堆疊 × 4）
    │
    ▼ AdaptiveAvgPool → flatten
    │
    ▼ FC(256, 512) → LayerNorm → 512d
```

**濾波器應用**（使用 PyTorch depthwise conv）：

```python
def _apply_srm(self, gray_tensor):
    # gray_tensor: (B, 1, H, W)
    # SRM 核：(30, 1, 3, 3)，weight 固定不更新
    noise = F.conv2d(
        gray_tensor,
        self.srm_kernels,  # requires_grad=False
        padding=1,
        groups=1
    )  # 輸出：(B, 30, H, W)
    return noise
```

---

## 4. 融合模組（Fusion Module）

**檔案**：`src/fusion/fusion_module.py`

### Cross-Attention Fusion 架構

融合的核心思想：將五個特徵流視為五個「token」，透過 Transformer Self-Attention 讓它們互相參考。

```
輸入：5 個 512d 特徵向量
    │
    ▼ 加上可學習的 Stream Embeddings (1, 5, 512)
      （讓模型知道哪個 token 來自哪個流）
    │
    ▼ Transformer Layer × 2：
      ┌─────────────────────────────────────┐
      │  MultiheadAttention(512d, 8 heads)  │
      │         ↓ Residual                  │
      │  LayerNorm                          │
      │         ↓                           │
      │  FFN: 512 → 2048 → 512 (GELU)      │
      │         ↓ Residual                  │
      │  LayerNorm                          │
      └─────────────────────────────────────┘
    │
    ▼ Flatten：5 × 512 = 2560d
```

**關鍵程式碼**：

```python
class CrossAttentionFusionLayer(nn.Module):
    def __init__(self, d_model=512, n_heads=8, dropout=0.1):
        self.self_attn = nn.MultiheadAttention(
            d_model, n_heads, dropout=dropout, batch_first=True
        )
        self.norm1 = nn.LayerNorm(d_model)
        self.norm2 = nn.LayerNorm(d_model)
        self.ffn = nn.Sequential(
            nn.Linear(d_model, d_model * 4),  # 4× expansion（標準 Transformer）
            nn.GELU(),
            nn.Dropout(dropout),
            nn.Linear(d_model * 4, d_model)
        )

    def forward(self, x):
        # x: (B, 5, 512) — 5 個流的 token sequence
        # Pre-norm 架構（比 Post-norm 更穩定）
        residual = x
        x = self.norm1(x)
        attn_out, attn_weights = self.self_attn(x, x, x)  # Q=K=V=x（Self-attention）
        x = residual + attn_out

        residual = x
        x = self.norm2(x)
        x = residual + self.ffn(x)
        return x, attn_weights
```

**Stream Embeddings 的作用**：

```python
class CrossAttentionFusion(nn.Module):
    def __init__(self):
        # 可學習的流識別嵌入（類似 BERT 的 token type embeddings）
        self.stream_embeddings = nn.Parameter(torch.randn(1, 5, 512))

    def forward(self, features):
        # features: list of 5 tensors, each (B, 512)
        x = torch.stack(features, dim=1)  # (B, 5, 512)
        x = x + self.stream_embeddings    # 加上流識別資訊
        # 過兩層 Transformer
        for layer in self.layers:
            x, _ = layer(x)
        return x.flatten(1)  # (B, 2560)
```

### 完整模型：AIImageDetector

```python
class AIImageDetector(nn.Module):
    def forward(self, images):
        # 1. 五個特徵提取器並行
        clip_feat   = self.clip_extractor(images)    # (B, 512)
        fft_feat    = self.fft_extractor(images)     # (B, 512)
        dct_feat    = self.dct_extractor(images)     # (B, 512)
        noise_feat  = self.noise_extractor(images)   # (B, 512)
        # DIRE 視 VRAM 是否足夠選擇性加入

        # 2. Cross-Attention 融合
        fused = self.fusion([clip_feat, fft_feat, dct_feat, noise_feat])  # (B, 2560)

        # 3. 共用 Backbone
        shared = self.backbone(fused)  # (B, 512)

        # 4. 雙頭輸出
        logits_binary = self.head_binary(shared)  # (B, 2): Real/Fake
        logits_source = self.head_source(shared)  # (B, 10): 生成器來源

        return logits_binary, logits_source
```

---

## 5. 梯度反轉層（GRL）與對抗訓練

**檔案**：`s3_main_grl.py`

### GRL 的直覺理解

**問題**：模型訓練後，特徵空間可能隱含「這張圖是哪個生成器產生的」的資訊，導致無法泛化到未見過的生成器。

**解法**：強迫 Backbone 學到「與生成器無關」的特徵。
- 在 Backbone 後接一個「生成器辨別器」
- 但反轉梯度方向，讓 Backbone 主動「忘掉」生成器資訊

### GRL 實作

```python
class _GRL(torch.autograd.Function):
    @staticmethod
    def forward(ctx, x, strength):
        ctx.save_for_backward(strength)
        return x.clone()           # Forward: 直接通過，不改變數值

    @staticmethod
    def backward(ctx, grad_output):
        strength, = ctx.saved_tensors
        return -strength * grad_output, None  # Backward: 反轉梯度！

def grad_reverse(x, strength=1.0):
    return _GRL.apply(x, torch.tensor(strength))
```

**為何能讓特徵「忘記」生成器？**

```
Backbone → [GRL，反轉梯度] → 生成器辨別器
                                    │ Loss: 辨別器分不清是哪個生成器
                                    ↓
                              梯度往 GRL 傳播
                                    │ GRL 反轉
                                    ↓
                         Backbone 的梯度被反轉
                         → Backbone 學到「讓辨別器困惑」的特徵
                         = 生成器無關特徵
```

### GRL 強度漸進排程

訓練初期不應讓 GRL 太強，否則 Backbone 還沒學會分辨 Real/Fake 就被迫忘掉生成器資訊：

```python
def get_grl_lambda(epoch, total_epochs, lambda_max=0.05, gamma=10.0):
    # Ganin et al. (2015) 漸進排程
    p = epoch / total_epochs           # p: 訓練進度 [0, 1]
    return lambda_max * (2 / (1 + np.exp(-gamma * p)) - 1)

# 效果：
# epoch 0   → λ ≈ 0    (GRL 幾乎無效)
# epoch 15  → λ ≈ 0.025 (中等對抗強度)
# epoch 30  → λ ≈ 0.05  (最大對抗強度)
```

### 完整 GRL 模型架構

```python
class FusionDetectorGRL(nn.Module):
    def __init__(self):
        # 共用 Backbone（繼承自 AIImageDetector）
        self.backbone = ...
        self.head_binary = nn.Linear(512, 2)    # Real/Fake
        self.head_source = nn.Linear(512, 10)   # 來源分類（多工）

        # 生成器辨別器（接在 GRL 之後）
        self.gen_discriminator = nn.Sequential(
            nn.Linear(512, 256),
            nn.ReLU(),
            nn.Dropout(0.3),
            nn.Linear(256, 12)   # 12 個 AI 生成器（不含 real）
        )

    def forward(self, features, grl_lambda=0.0):
        shared = self.backbone(fused)

        logits_binary = self.head_binary(shared)
        logits_source = self.head_source(shared)

        # GRL 分支：只用 Fake 樣本訓練
        reversed_feat = grad_reverse(shared, grl_lambda)
        logits_gen = self.gen_discriminator(reversed_feat)

        return logits_binary, logits_source, logits_gen
```

### 損失函數

```
Total Loss = CE(logits_binary, labels)              ← 主要任務：Real/Fake
           + 0.1 × CE(logits_source, source_labels)  ← 輔助任務：來源辨識
           + λ_grl × CE(logits_gen[fake], gen_ids[fake])  ← 對抗任務：生成器辨別
```

- `λ_grl` 由漸進排程控制（0 → 0.05）
- 生成器辨別損失只計算 Fake 樣本（真實照片沒有生成器 ID）

---

## 6. 訓練流程詳解

**檔案**：`src/fusion/trainer.py`

### 訓練配置

| 參數 | 值 | 說明 |
|------|-----|------|
| EPOCHS | 30 | 總訓練輪數 |
| BATCH_SIZE | 256 | 批次大小（特徵訓練） |
| LR | 1e-3 | 學習率（AdamW） |
| WARMUP_EPOCHS | 5 | 線性暖身期 |
| CLIP_GRAD | 1.0 | 梯度裁剪上限 |
| DROPOUT | 0.3 | Backbone/Head dropout |

### 優化器配置（差異化學習率）

```python
def build_optimizer_scheduler(model, lr=1e-3):
    # CLIP 投影層用較低的學習率（10 倍差距）
    # 因為 CLIP 特徵已足夠好，不需要大幅調整
    param_groups = [
        {
            'params': model.clip_extractor.projection.parameters(),
            'lr': lr * 0.1    # 10× 降低 CLIP 投影層 LR
        },
        {
            'params': [p for name, p in model.named_parameters()
                      if 'clip_extractor.projection' not in name],
            'lr': lr
        }
    ]
    optimizer = torch.optim.AdamW(param_groups, weight_decay=1e-4)

    # Cosine Annealing 排程（含線性 Warmup）
    scheduler = CosineAnnealingLR(optimizer, T_max=total_epochs)
    return optimizer, scheduler
```

### 混合精度訓練（AMP）

```python
scaler = torch.cuda.amp.GradScaler()

with torch.cuda.amp.autocast():    # 自動轉換為 fp16 計算
    outputs = model(inputs)
    loss = criterion(outputs, targets)

scaler.scale(loss).backward()      # 縮放後反向傳播（防止 fp16 下溢）
scaler.unscale_(optimizer)
torch.nn.utils.clip_grad_norm_(model.parameters(), CLIP_GRAD)  # 梯度裁剪
scaler.step(optimizer)
scaler.update()
```

### 課程學習（Curriculum Learning）

按照樣本難度分三階段訓練（主要訓練流程使用，GRL 訓練不啟用）：

```
Phase 1（Epoch 1-10）：  只用「Easy」樣本（ELA 變異量高，壓縮痕跡明顯）
Phase 2（Epoch 11-20）： 加入「Medium」樣本
Phase 3（Epoch 21-30）： 加入所有「Hard」樣本（壓縮痕跡不明顯）
```

**難度判斷邏輯**（`difficulty_classifier.py`）：

```python
def classify_difficulty(image_path):
    # 方法1：ELA 方差（JPEG 壓縮分析）
    ela = compute_ela(image_path)
    ela_variance = np.var(ela)

    # 方法2：FFT 高頻能量
    fft = np.fft.fft2(grayscale)
    high_freq_energy = np.mean(np.abs(fft[H//4:3*H//4, W//4:3*W//4]))

    # 啟發式規則
    if ela_variance > HIGH_THRESHOLD:
        return 'easy'    # 壓縮痕跡明顯 → 容易辨識
    elif ela_variance < LOW_THRESHOLD and high_freq_energy > FREQ_THRESHOLD:
        return 'hard'    # 幾乎無壓縮痕跡且高頻豐富 → 難
    else:
        return 'medium'
```

### 早停（Early Stopping）

```python
# patience=10：若 validation accuracy 連續 10 個 epoch 不改善則停止
if val_acc > best_val_acc:
    best_val_acc = val_acc
    torch.save(model.state_dict(), 'best_model.pth')
    patience_counter = 0
else:
    patience_counter += 1
    if patience_counter >= 10:
        print("Early stopping triggered!")
        break
```

---

## 7. 實驗流程（10 個階段）

### Stage 1：EXP-A — 單流基準測試

**檔案**：`s3a_single_stream.py`

**目標**：建立每個特徵流的單獨基準性能

**兩個階段**：

**Phase 1：特徵提取與快取**
```
for stream in [CLIP, FFT, DCT, DIRE, Noise]:
    for batch in train_dataloader:
        features = stream.extract(images)  # (B, 512)
        save to disk: outputs/exp_a/features/{stream}_train.pt
```

- 批次大小：DIRE=16（VRAM 不足時），其餘=64
- 增強：JPEG 再壓縮、resize、模糊、加噪（各 50% 機率）
- **目的**：快取特徵，後續實驗直接讀取，不需重新提取（省時）

**Phase 2：訓練單流分類頭**
```python
# 輕量分類頭（不是完整模型）
head = nn.Sequential(
    nn.Linear(512, 256),
    nn.ReLU(),
    nn.Dropout(0.3),
    nn.Linear(256, 2)
)
# 訓練 20 epochs，LR=1e-3
```

**輸出**：每個流的 Accuracy/AUC/F1 基準值

---

### Stage 2：EXP-B — 消融研究

**檔案**：`s3b_ablation.py`

**目標**：找出最佳特徵流組合

**Part 1：組合研究（26 種組合）**

```python
from itertools import combinations

all_combinations = []
for r in range(2, 6):  # 2 到 5 個流的組合
    all_combinations.extend(combinations(STREAMS, r))
# C(5,2)+C(5,3)+C(5,4)+C(5,5) = 10+10+5+1 = 26 種

for combo in all_combinations:
    # 讀取快取特徵，直接 concat
    features = [cached_features[s] for s in combo]
    x = torch.cat(features, dim=1)   # (B, N×512)

    # 簡單 MLP 分類頭
    mlp = nn.Sequential(
        nn.Linear(len(combo)*512, 512),
        nn.ReLU(),
        nn.Linear(512, 2)
    )
    # 訓練 15 epochs
```

**Part 2：Leave-One-Out（LOO）**

```python
# 訓練 5 個模型，每個各去掉一個流
for excluded in STREAMS:
    remaining = [s for s in STREAMS if s != excluded]
    # → 得出去掉哪個流最傷性能
```

**輸出**：`outputs/exp_b/results.json`（包含所有組合指標，找出最關鍵的流）

---

### Stage 3：EXP-C — 融合策略比較

**檔案**：`s3c_fusion_compare.py`

**目標**：驗證 Cross-Attention Fusion 優於其他融合方式

**三種策略**：

```python
# 策略 1：Concat + MLP
fused = torch.cat(features, dim=1)  # (B, 2560)
output = MLP(fused)                 # 2560 → 1024 → 512 → 2

# 策略 2：加權融合（學習每個流的重要性）
weights = softmax(learnable_weights)  # (5,)
fused = sum(w * f for w, f in zip(weights, features))  # (B, 512)
output = classifier(fused)

# 策略 3：Cross-Attention（本專案方法）
fused = cross_attention_transformer(features)  # (B, 2560)
output = backbone(fused)  # → 2
```

**公平比較條件**：相同訓練集、相同 20 epochs、相同特徵（從 EXP-A 快取讀取）

**補充實驗**：`s3c_add_g2.py` — 在 G2（跨生成器）測試集上追加評估

---

### Stage 4：主訓練 — FusionDetector + GRL

**檔案**：`s3_main_grl.py`

**目標**：訓練最終模型（加入 GRL 對抗訓練）

**訓練流程**：

```
for epoch in range(30):
    λ_grl = get_grl_lambda(epoch, 30, lambda_max=0.05)

    for batch in dataloader:
        images, labels, source_labels, gen_ids = batch

        # Forward
        logits_bin, logits_src, logits_gen = model(features, λ_grl)

        # Loss
        loss_bin = CE(logits_bin, labels)
        loss_src = CE(logits_src, source_labels)

        fake_mask = (labels == 1)
        loss_gen = CE(logits_gen[fake_mask], gen_ids[fake_mask])

        loss = loss_bin + 0.1*loss_src + λ_grl*loss_gen

        # Backward（AMP）
        scaler.scale(loss).backward()
        ...
```

**輸出**：
- `outputs/main_grl/best_model.pth`（最佳模型）
- `outputs/main_grl/training_history.json`（損失/準確率曲線）
- `outputs/main_grl/final_results.json`（最終測試集指標）

---

### Stage 5：EXP-D — 泛化能力測試

**檔案**：`s4d_grl_generalization.py`

**目標**：測試模型對未見過生成器的泛化

**三個測試集**：

| 測試集 | 說明 | 目的 |
|--------|------|------|
| G1 | 標準測試集（訓練集同分布） | 基本性能 |
| G2 | 跨生成器測試集（未見生成器） | 泛化能力 |
| G3 | 各生成器逐一分析 | 找出弱點 |

**Per-Generator 分析**：

```python
# G3：對每個生成器 ID 單獨計算 accuracy
results_per_gen = {}
for gen_id in range(19):
    mask = (test_gen_ids == gen_id)
    if mask.sum() > 0:
        acc = (predictions[mask] == labels[mask]).float().mean()
        results_per_gen[ID_TO_GENERATOR[gen_id]] = acc.item()
# → 找出哪些生成器最難偵測
```

---

### Stage 6：EXP-F — SOTA 比較

**檔案**：`s4f_sota_compare_v2.py`

**目標**：與現有最先進方法公平比較

**四個比較對象**：

| 方法 | 架構 | 特點 |
|------|------|------|
| ResNet50 | 標準 CNN（完整微調） | 最常見的基準線 |
| EfficientNet-B4 | 效率型 CNN | 參數效率高 |
| CLIP Linear Probe | CLIP + 線性頭（UnivFD 風格） | 純語意特徵 |
| **FusionDetectorGRL** | 本方法 | 五流+GRL |

**公平設置**：
- 所有方法使用相同訓練集（train.csv）
- 相同測試集（test.csv）
- ResNet/EfficientNet：最多 10 epochs 微調，最多 20k 訓練樣本
- 相同圖像前處理

**輸出**：`outputs/exp_f_v2/sota_comparison.json`（含推論時間、參數量）

---

### Stage 7：EXP-G — 魯棒性測試

**檔案**：`s4g_robustness_test.py`

**目標**：測試在各種圖像破壞下的性能

**13 種破壞類型**：

```python
DEGRADATIONS = [
    ('original', lambda x: x),           # 無破壞（基準）
    ('jpeg_70',  lambda x: jpeg_compress(x, q=70)),  # JPEG Q70
    ('jpeg_50',  lambda x: jpeg_compress(x, q=50)),  # JPEG Q50
    ('jpeg_30',  lambda x: jpeg_compress(x, q=30)),  # JPEG Q30（重壓縮）
    ('resize_75', lambda x: resize_pct(x, 0.75)),    # 縮放 75%
    ('resize_50', lambda x: resize_pct(x, 0.50)),    # 縮放 50%
    ('resize_25', lambda x: resize_pct(x, 0.25)),    # 縮放 25%
    ('blur_1',    lambda x: gaussian_blur(x, σ=1)),  # 輕微模糊
    ('blur_2',    lambda x: gaussian_blur(x, σ=2)),  # 中度模糊
    ('blur_3',    lambda x: gaussian_blur(x, σ=3)),  # 強模糊
    ('noise_10',  lambda x: add_noise(x, σ=10)),     # 輕微加噪
    ('noise_25',  lambda x: add_noise(x, σ=25)),     # 中度加噪
    ('noise_50',  lambda x: add_noise(x, σ=50)),     # 強加噪
]
```

**輸出**：`outputs/exp_g/robustness_results.json` + 折線圖（各破壞強度 vs 準確率）

---

### Stage 8：EXP-H — NSS 驗證

**檔案**：`s4h_nss_experiment.py`

**目標**：驗證自然場景統計（Natural Scene Statistics, NSS）假說

**NSS 假說**：真實照片服從特定統計分布（如 Laplacian 對比度、MSCN 係數），AI 圖像偏離此分布。

**分析步驟**：
1. 提取 NSS 特徵（Laplacian 對比度、熵、高頻能量）
2. 繪製 Real vs. Fake 的分布直方圖
3. 統計顯著性檢定（KS test）
4. 驗證模型是否學到了這些 NSS 差異

---

### Stage 9：EXP-I — 多種子驗證

**檔案**：`s4i_multi_seed.py`

**目標**：驗證模型性能的穩定性（排除「偶然」好結果）

```python
SEEDS = [42, 123, 2024, 7, 99]

results = []
for seed in SEEDS:
    set_seed(seed)    # 固定所有隨機源（torch, numpy, random, CUDA）
    model = train_grl_model()
    metrics = evaluate(model, test_set)
    results.append(metrics)

# 回報：mean ± std
print(f"Accuracy: {np.mean(accs):.4f} ± {np.std(accs):.4f}")
```

---

### Stage 10：補充實驗 — 可解釋性與可視化

**檔案**：`s4_supplementary.py`

**可選步驟**：

**Step 5：Grad-CAM 視覺化**
```bash
python s4_supplementary.py --steps 5
```
- 對 Real/Fake 圖像各取樣數張
- 計算每個特徵流的 Grad-CAM 熱力圖
- 顯示模型「看哪裡」做決策

**Step 6：Cross-Attention 權重分析**
```bash
python s4_supplementary.py --steps 6
```
- 提取 Fusion 層的 attention weights
- 比較 Real vs. Fake 的 attention 模式
- 顯示哪些流最受關注（attention 峰值）

**Step 8：頻率譜可視化**
```bash
python s4_supplementary.py --steps 8
```
- 繪製 GAN vs. Diffusion vs. Real 的平均 FFT 頻率譜
- 可視化不同生成技術的頻率特徵差異

**Step 11：錯誤分析**
```bash
python s4_supplementary.py --steps 11
```
- 在 G2 測試集上分析 False Positive 和 False Negative
- 找出常見的誤判模式
- 輸出：`outputs/supplementary/error_analysis.json`

---

## 8. 資料集結構

### CSV 格式

```
path,label,generator,difficulty
/data/wildfake/fake/img001.jpg,1,sdv5,medium
/data/wildfake/real/img002.jpg,0,real,easy
/data/wildfake/fake/img003.jpg,1,midjourney,hard
```

| 欄位 | 說明 |
|------|------|
| `path` | 圖像絕對路徑 |
| `label` | 0=真實照片, 1=AI 生成 |
| `generator` | 生成器名稱（字串） |
| `difficulty` | 樣本難度（easy/medium/hard） |

### 生成器 ID 映射（共 19 個）

```python
GENERATOR_TO_ID = {
    # Diffusion 模型（訓練集見過，ID 0-4）
    'adm': 0, 'glide': 1, 'sdv4': 2, 'sdv5': 3, 'midjourney': 4,

    # 混合生成器（訓練集見過，ID 5-9）
    'wildfake': 5, 'biggan': 6, 'vqdm': 7, 'wukong': 8, 'firefly': 9,

    # 真實照片（ID 10-11）
    'real': 10, 'real_extra': 11,

    # GAN 類（未見過，ID 12-18）— G2/G3 測試使用
    'wildfake_ddim': 12, 'wildfake_other': 13,
    'stylegan': 14, 'dcgan': 15, 'dcgan_unseen': 16,
    'fursona_gan': 17, 'waifu_gan': 18,
}
```

- **訓練集**：使用 ID 0-9（10 種 AI 生成器）+ 真實照片
- **泛化測試**：加入 ID 12-18（7 種未見過的 GAN）

### PyTorch Dataset

**檔案**：`src/data_preparation/dataset.py`

```python
class DeepfakeDataset(Dataset):
    def __getitem__(self, idx):
        row = self.df.iloc[idx]
        image = Image.open(row['path']).convert('RGB')

        if self.augment:
            image = self.augmentations(image)

        image = self.transform(image)  # Resize, ToTensor, Normalize

        label = torch.tensor(row['label'], dtype=torch.long)
        gen_id = GENERATOR_TO_ID.get(row['generator'], -1)
        difficulty = DIFFICULTY_MAP.get(row['difficulty'], 1)

        return image, label, gen_id, difficulty
```

---

## 9. 關鍵超參數總表

### 模型架構

| 組件 | 參數 | 值 |
|------|------|-----|
| Cross-Attention | `d_model` | 512 |
| | `n_heads` | 8 |
| | `n_layers` | 2 |
| | FFN 倍數 | 4× |
| Backbone | 輸入維度 | 2560 (5×512) |
| | 中間層 | 2560→1024→512 |
| | Dropout | 0.3 |

### 訓練配置

| 參數 | 值 | 說明 |
|------|-----|------|
| EPOCHS | 30 | 訓練輪數 |
| BATCH_SIZE | 256 | 特徵訓練批次 |
| IMG_BATCH | 64 | 特徵提取批次 |
| DIRE_BATCH | 16 | DIRE 提取批次（VRAM 限制） |
| LR | 1e-3 | AdamW 學習率 |
| CLIP_LR_RATIO | 0.1 | CLIP 投影層學習率比例 |
| WARMUP_EPOCHS | 5 | 線性暖身期 |
| CLIP_GRAD | 1.0 | 梯度裁剪上限 |
| Weight Decay | 1e-4 | AdamW 正則化 |

### GRL 配置

| 參數 | 值 | 說明 |
|------|-----|------|
| LAMBDA_GRL | 0.05 | 最大對抗權重 |
| LAMBDA_SRC | 0.1 | 來源分類損失權重 |
| GRL_GAMMA | 10.0 | 排程增長速率 |
| Gen Disc 輸出 | 12 | AI 生成器類別數 |

---

## 10. 程式執行順序

建議依以下順序執行：

```bash
# 1. 提取五流特徵並快取（最耗時，但後續可複用）
python s3a_single_stream.py

# 2. 流組合消融研究（使用快取特徵）
python s3b_ablation.py

# 3. 融合策略比較
python s3c_fusion_compare.py
python s3c_add_g2.py     # 追加 G2 評估

# 4. 主模型訓練（GRL）
python s3_main_grl.py

# 5. 泛化測試（需要 main_grl/best_model.pth）
python s4d_grl_generalization.py

# 6. SOTA 比較
python s4f_sota_compare_v2.py

# 7. 魯棒性測試
python s4g_robustness_test.py

# 8. NSS 驗證
python s4h_nss_experiment.py

# 9. 多種子驗證
python s4i_multi_seed.py

# 10. 可解釋性視覺化
python s4_supplementary.py --steps 5 6 8 11
```

**互動式 Demo**：

```bash
python ai_detector_demo.py --image /path/to/image.jpg
# 輸出：各流信心分數 + Grad-CAM 熱力圖 + 最終 Real/Fake 判斷
```

---

## 附錄：輸出目錄結構

```
outputs/
├── exp_a/
│   ├── features/
│   │   ├── clip_train.pt          # CLIP 訓練集特徵 (N, 512)
│   │   ├── fft_train.pt           # FFT 訓練集特徵
│   │   └── ...                    # 其餘流 × {train, val, test}
│   ├── clip/best_model.pth        # 單流分類器
│   └── ...
├── exp_b/results.json             # 26 組合 + 5 LOO 結果
├── exp_c/results.json             # 3 種融合策略比較
├── main_grl/
│   ├── best_model.pth             # 最佳模型（主要 checkpoint）
│   ├── training_history.json      # Loss/Acc 曲線
│   └── final_results.json         # 測試集最終指標
├── exp_d_grl/
│   ├── generalization_results.json  # G1/G2/G3 結果
│   └── per_sample_predictions.csv   # 每張圖的預測
├── exp_f_v2/sota_comparison.json   # 與 SOTA 的比較
├── exp_g/robustness_results.json   # 魯棒性測試
├── exp_h/nss_results.json          # NSS 分析
├── exp_i/multi_seed_results.json   # 多種子統計
└── supplementary/
    ├── gradcam_*.png               # Grad-CAM 熱力圖
    ├── spectrum_*.png              # 頻率譜圖
    └── error_analysis.json         # FP/FN 分析
```
